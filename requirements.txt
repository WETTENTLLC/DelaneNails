# Core dependencies
flask==2.0.1
fastapi==0.75.0
uvicorn==0.17.6
requests==2.28.1
python-dotenv==0.20.0
pydantic==1.9.0  # Using older version that doesn't require Rust
flask-socketio==5.3.2

# Testing tools
pytest==7.0.1

# Optional communication libraries
# twilio==8.5.0  # Uncomment if needed for SMS
